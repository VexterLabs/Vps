name: Ubuntu-Testing

on:
  pull_request:
    paths:
     # - '**.jpg'
      #- '**.jpeg'
      #- '**.png'
      - '**.bat'
  push:
    branches:
      - main
    paths:
      #- '**.jpg'
      #- '**.jpeg'
      #- '**.png'
      - '**.bat'
  workflow_dispatch:
  schedule:
  - cron: "0 */24 * * *"
jobs:
   build:
    name: Machine running...
    runs-on: ubuntu-latest
    timeout-minutes: 10080
    
    steps:
      - name: Time Counter
        run: python time.py
        
      - name: Sleep
        shell: bash
        run: sleep 15s
